def func(pkg): 
    if(pkg==0):
        print("No package")
        return 0
    elif:
        print("Package Initiated")
        return(pkg%100)

def comp(x):
    compare_1_coeff = wavelet(x)
    compare_2_coeff = vector(x)
    z compare(compare_1_coeff,compare_2_coeff)
    return comp

def int_massive(pkg):
    x = func(pkg)
    y = comp(x)
    z = perf(y)

